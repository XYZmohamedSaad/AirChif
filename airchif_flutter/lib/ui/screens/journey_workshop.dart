import 'package:flutter/material.dart';

class JourneyWorkshopScreen extends StatelessWidget {
  static const routePath = '/journey';

  const JourneyWorkshopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Journey Workshop – coming soon")),
    );
  }
}
